<apex:page controller="Arrays">
    <apex:form >
        <apex:pageBlock >
        <apex:pageBlockTable value="{!accuracy}" var="acc">
            <apex:column value="{!acc.name}"/>
            <apex:column value="{!acc.rating}"/>
            <apex:column value="{!acc.Industry}"/>
        </apex:pageBlockTable>
            </apex:pageBlock>
    </apex:form>
</apex:page>